'use client';

import 'mapbox-gl/dist/mapbox-gl.css';
import CustomMap from '@/components/ui/map/customMap';

export default function MapView() {
  return (
    <div>
      <CustomMap />
    </div>
  );
}
